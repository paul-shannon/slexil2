<script src="https://d3js.org/d3.v7.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/d3-cloud@1.2.7/build/d3.layout.cloud.js"></script>
<span style="margin-left: 30px;">
   <button id="showCloudButton" class="standardSlexilButton"> Word Cloud
  </button>
</span>

<div id="cloudDialog"
        style="margin: 5px; width: 700px;">
  <div id="cloud-container">
    <svg id="cloud-svg" width="600" height="600"></svg>
  </div>
  <div id="info-panel" style="margin: 15px; padding: 10px; height: 120px; border: 1px solid green;">
       <p>Click on any word root above</p>
  </div>
  <div> 
     <button id="showAllLinesButton"
             class="standardSlexilButton"> Show All Lines</button>
     <button id="showOnlyRootMatchesButton"
             class="standardSlexilButton"> Show Matched Lines</button>        
  </div>

</div>
<script>
//--------------------------------------------------------------------------------
class Radicals {

  data = [{"radical":"ləx̌","definition":"be light, be bright","lines":[21,59,94,97,99,101,104,105]},
          {"radical":"bəsad","definition":"be dark","lines":[2,22,55]},
          {"radical":"had","definition":"talk","lines": [3]},
          {"radical":"čal","definition":"chase; catch.","lines":[16,69,71,81,83,84,85,89]},
          {"radical":"kʷəd","definition":"be taken","lines":[3,4,36,37,46,74,75,79,96]},
          {"radical":"q̓xʷ", "definition":"be upstream","lines":[4,19]},
          {"radical":"huyu", "definition": "do, make, prepare", "lines": [5,7,8,12,43,45,50,51,102]},
          {"radical":"c̓agʷa", "definition": "washed", "lines": [6]},
          {"radical":"xʷəxʷaʔxʷəʔ", "definition": "be swift", "lines": [7,71,75,83,85]},
          {"radical":"təlawil", "definition": "run a distance", "lines": [7,38,83]},
          {"radical":"t̓əd", "definition": "be in a row, lined up", "lines": [9]},
          {"radical":"tədᶻil", "definition": "lie in bed", "lines": [47,60,61,105]},
          {"radical":"saxʷəb", "definition": "jump", "lines": [9,11,13,15,60,68,72,77,82]},
          {"radical":"x̌ək̓ʷ", "definition": "overturned", "lines": [10,13,14]},
          {"radical":"cut", "definition": "speak", "lines": [16,30,34,41,47,48,88,93]},
          {"radical":"ʔux̌ʷ", "definition": "go", "lines": [17,18,19,27,28,33,38,45,57,66,67,90]},
          {"radical":"gʷaʔxʷ", "definition": "walk", "lines": [18,19,24,25]},
          {"radical":"ʔuluɬ", "definition": "travel by water", "lines": [18,86,87]},
          {"radical":"č̓it", "definition": "be near", "lines": [20,23,91,94]},
          {"radical":"hay", "definition": "know", "lines": [23,27,28,32]},
          {"radical":"ʔa", "definition": "be there", "lines": [16,20,26]},
          {"radical":"ʔiwəd", "definition": "decide", "lines": [27]},
          {"radical":"dukʷ", "definition": "be abnormal", "lines": [28]},
          {"radical":"qada", "definition": "steal", "lines": [29]},
          {"radical":"hədʔiw̓", "definition": "be indoors", "lines": [30,32]},
          {"radical":"kayiɬ", "definition": "pretend", "lines": [31]},
          {"radical":"qʷəlub", "definition": "have gray hair", "lines": [31,39]},
          {"radical":"ck̓usəd", "definition": "use walking stick", "lines": [32,39]},
          {"radical":"ʔəƛ̓", "definition": "come", "lines": [35,44,100,103]},
          {"radical":"gʷəč̓", "definition": "search", "lines": [44,100]},
          {"radical":"ɬaq̓", "definition": "fall,lie,lay", "lines": [43,44,50]},
          {"radical":"haʔɬ", "definition": "be good,nice, beautiful", "lines": [45,116]},
          {"radical":"ʔalq̓ʷ", "definition": "be away from fire", "lines": [47]},
          {"radical":"šədᶻal", "definition": "go outdoors", "lines": [48]},
          {"radical":"ɬax̌", "definition": "be dark", "lines": [48,54,66,97,101,104]},
          {"radical":"ləq̓aɬ", "definition": "be in correct place", "lines": [49,50,65]},
          {"radical":"wač", "definition": "watch", "lines": [51,52,56,96]},
          {"radical":"ƛ̓ip̓", "definition": "grip,squeeze", "lines": [36,53]},
          {"radical":"ƛ̓uc̓", "definition": "pull together, bunch up", "lines": [55]},
          {"radical":"ɬaʔ", "definition": "arrive there", "lines": [58,67]},
          {"radical":"puʔu", "definition": "blow", "lines": [62]},
          {"radical":"pəd", "definition": "buried", "lines": [62,65]},
          {"radical":"ɬaɬlil", "definition": "live", "lines": [26]},
          {"radical":"wəq̓", "definition": "blink", "lines": [64]},
          {"radical":"šuɬ", "definition": "look,see", "lines": [40,65]},
          {"radical":"bəč ", "definition": "fall", "lines": [65]},
          {"radical":"pəkʷib", "definition": "snatch, peel off", "lines": [68]},
          {"radical":"ɬčil", "definition": "arrive", "lines": [25,36,38,40,72,86,91,95]},
          {"radical":"ʔix̌ʷ", "definition": "throw away", "lines": [73,80]},
          {"radical":"šəq", "definition": "be high", "lines": [74,78,82]},
          {"radical":"č̓əlp", "definition": "twist, turn", "lines": [74]},
          {"radical":"qʷiq̓ʷ", "definition": "be strong", "lines": [76]},
          {"radical":"saq̓ʷ", "definition": "fly", "lines": [78]},
          {"radical":"q̓ʷəɬəb", "definition": "be tired", "lines": [80]},
          {"radical":"lil", "definition": "be far", "lines": [59,63,81]},
          {"radical":"q̓il", "definition": "be on board", "lines": [86]},
          {"radical":"qʷšab", "definition": "be foggy", "lines": [87,88,89]},
          {"radical":"laʔ", "definition": "locate, point out", "lines": [88]},
          {"radical":"gʷəq", "definition": "be bright", "lines": [92]},
          {"radical":"ʔitut", "definition": "sleep", "lines": [98]},
          {"radical":"šəɬ", "definition": "make", "lines": [1021]},
          {"radical":"šac̓", "definition": "finished", "lines": [106]}
         ];

    constructor(){
       console.log("Radicals class ctor");
       window.data = this.data;
       }

    getRoots(){
      return(this.data.map(function(x){return x.radical}))
      }
    getCount(radical){
      const entry = this.data.filter(function(x){return(x.radical == radical)})
      return(entry[0]['lines'].length)
      }
      
    hiddenrenderWordCloud(){
      var canvas = document.getElementById('wordcloud-canvas');
      window.canvas = canvas;
      const ctx = canvas.getContext('2d')
      window.ctx = ctx;
      var infoPanel = document.getElementById('info-panel');
      var self = this

      const radicals = new Radicals();
      const seen = new Map();
      radicals.data.forEach(d => {
         console.log(d)
         const count = d.lines.length;
         if (!seen.has(d.radical) || seen.get(d.radical).count < count) {
           seen.set(d.radical,
              {text: d.radical, definition: d.definition, count, lines: d.lines});
              }
         }); // forEach
      const words = Array.from(seen.values()).map(d => ({
         text:       d.text,
         definition: d.definition,
         count:      d.count,
         lines:      d.lines,
         size:       12 + d.count * 7   // font size proportional to frequency
         }));
       } // renderWordCloud
} // class Radicals      
//--------------------------------------------------------------------------------
</script>


<script>
    function seededRandom(seed) {
      let s = seed >>> 0;
      return function () {
        s = Math.imul(s, 1664525) + 1013904223 >>> 0;
        return s / 4294967296;
      };
    }

    const WIDTH  = 600;
    const HEIGHT = 500;
    const SEED   = 42;

      // ── Colour scale: blue (rare) → amber (frequent) ──
    const colorScale = d3.scaleSequential()
      .domain([1, 9])
      .interpolator(d3.interpolateRainbow);

      // ── Build word list from Radicals data ──
    const radicals = new Radicals();
    const seen = new Map();
    radicals.data.forEach(d => {
      const count = d.lines.length;
      console.log("--- d");
      console.log(d)
      if (!seen.has(d.radical) || seen.get(d.radical).count < count) {
         seen.set(d.radical,
            {text: d.radical, definition: d.definition, count, lines: d.lines});
            }
          }); // forEach
      const words = Array.from(seen.values()).map(d => ({
         text:       d.text,
         definition: d.definition,
         count:      d.count,
         lines:      d.lines,
         size:       12 + d.count * 7   // font size proportional to frequency
         }));

    function draw(laid_out_words) {
      const svg = d3.select('#cloud-svg');
      svg.selectAll('*').remove();

      svg.append('g')
        .attr('transform', `translate(${WIDTH / 2},${HEIGHT / 2})`)
        .selectAll('text')
        .data(laid_out_words)
        .enter().append('text')
          .attr('class', 'word-text')
          .style('font-size',   d => `${d.size}px`)
          .style('font-family', 'Arial, sans-serif')
          .style('font-weight', d => d.count >= 6 ? 'bold' : 'normal')
          .style('fill',        d => colorScale(d.count))
          .attr('text-anchor', 'middle')
          .attr('transform',   d => `translate(${d.x},${d.y}) rotate(${d.rotate})`)
          .text(d => d.text)
          .on('click', (event, d) => {
             console.log("d: " + d);
             console.log(d);
             window.lineNumbers = d.lines;
             d3.select('#info-panel').html(
               `<strong>${d.text}</strong> &nbsp;—&nbsp; ${d.definition}<br>
               <span style="color:#555;font-size:20px;">
                  appears <strong>${d.count}</strong>
                 time${d.count !== 1 ? 's' : ''} in the corpus</span>`
            );
          })
          .on('mouseenter', function () { d3.select(this).style('opacity', 0.65); })
          .on('mouseleave', function () { d3.select(this).style('opacity', 1); });
    }

    // ── Layout builder — always uses the same seed ──
    function buildCloud() {
      d3.layout.cloud()
        .size([WIDTH, HEIGHT])
        .words(words.map(d => Object.assign({}, d)))   // fresh copy each run
        .padding(8)
        .rotate(0)                                      // keep all horizontal
        .font('Arial')
        .fontSize(d => d.size)
        .random(seededRandom(SEED))                     // ← reproducibility key
        .on('end', draw)
        .start();
    }

 buildCloud();


$("#showOnlyRootMatchesButton").on("click", function(){
  for (let i = 1; i <= 116; i++){
     x='#'+i;
     $(x).hide()
     }
  $(".html").hide()
  const matches = window.lineNumbers;
  //console.log("lines to show:");
  //console.log(matches)
  for(let i=0; i < matches.length; i++){
     x='#'+ matches[i];
     $(x).show()
     }
   }) // showOnlyRootMatchesButton

$("#showAllLinesButton").on("click", function(){
  for (let i = 1; i <= 116; i++){
     x='#'+i;
     $(x).show()
     $(".html").show()
     }
  }) // showAllLinesButton


$(document).ready(function(){
   $("#cloudDialog").dialog({title: "Explore Verb Roots",
                             autoOpen: false,
                             width: 700});
  $("#showCloudButton").on('click', function(){
     console.log("open dialog button click");
     const app = new Radicals()
     buildCloud()
     $("#cloudDialog").dialog("open")
     })
   })


//--------------------------------------------------------------------------------

</script>

